# FREE Tier Single-Service Render Deployment for StudyMateAI
# Uses only FREE features - No payment required!
# ONE URL for entire application - No Docker needed

services:
  # Single FREE Service - Spring Boot serving both API and Frontend
  - type: web
    name: studymate-app
    runtime: java
    plan: free
    buildCommand: |
      echo "Building StudyMateAI for FREE deployment..."
      # Build frontend
      cd frontend
      npm install
      npm run build
      echo "Frontend build complete"
      # Copy frontend to backend resources
      mkdir -p ../backend/src/main/resources/static
      cp -r build/* ../backend/src/main/resources/static/
      echo "Frontend copied to backend static resources"
      # Build backend with embedded frontend
      cd ../backend
      mvn clean package -DskipTests
      echo "Backend build complete with embedded frontend"
    startCommand: java -jar backend/target/*.jar
    healthCheckPath: /api/actuator/health
    autoDeploy: true
    envVars:
      - key: PORT
        value: 10000
      - key: SPRING_PROFILES_ACTIVE
        value: production
      - key: SPRING_DATASOURCE_URL
        value: "jdbc:postgresql://aws-0-ap-south-1.pooler.supabase.com:5432/postgres"
      - key: SPRING_DATASOURCE_USERNAME
        value: "postgres.vfhswjfjwqjmpcxjgedc"
      - key: SPRING_DATASOURCE_PASSWORD
        value: "ahtasham2105067"
      - key: JWT_SECRET
        value: "studymate-free-jwt-secret-key-2024-render-single-url"
      - key: JWT_EXPIRATION
        value: "86400000"
      - key: GEMINI_API_KEY
        value: "AIzaSyA2DsN6bTwwuySdLO-VkPwuaKBW19ShbSk"
      - key: GEMINI_API_URL
        value: "https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash-latest:generateContent"
      - key: SPRING_JPA_HIBERNATE_DDL_AUTO
        value: "update"
      - key: SPRING_JPA_SHOW_SQL
        value: "false"
      - key: CORS_ALLOWED_ORIGINS
        value: "*"
      - key: FILE_UPLOAD_DIR
        value: "/tmp/uploads"
      - key: MAX_FILE_SIZE
        value: "10MB"
      - key: MAX_REQUEST_SIZE
        value: "10MB"
# FREE Tier Single URL Deployment
# 🌐 Complete App: https://studymate-app.onrender.com
# 📱 Frontend: https://studymate-app.onrender.com/ (React UI)
# 🔌 Backend API: https://studymate-app.onrender.com/api/ (Spring Boot)
# 🔄 WebSocket: wss://studymate-app.onrender.com/api/ws/ (Real-time)
# 📊 Health: https://studymate-app.onrender.com/api/actuator/health
#
# ✅ FREE Tier Features Used:
# - Java runtime (FREE)
# - 750 hours/month (FREE)
# - Single service (saves resources)
# - No Docker (not needed on free tier)
# - Embedded frontend in Spring Boot
# - Supabase database (already free)
#
# ⚠️ FREE Tier Limitations:
# - Service sleeps after 15 minutes of inactivity
# - Cold start delay (30-60 seconds)
# - File uploads to /tmp (ephemeral storage)
# - 512MB RAM limit
# - No custom domains
